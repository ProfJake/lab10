doctype html
html(lang="en")
  head
      style
        include styles.css
	
      title= "The Activity Web Server"
  body
  h1 The Activity Express Web Server
  aside
    if trustedUser 
      a(href="/users/" + trustedUser._id) My Page
    
    block content

  block footer
   
      a(href='/') Home
      |
      |
      a(href='/search') Search
      |
      |
      a(href='/insert') Insert 

